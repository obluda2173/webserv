server {
       server_name example.org;
       listen      8080;
       root        /var/www/html;
}

server {
       server_name test.org;
       listen      8080;
       root        /var/www/images;
}

server {
       server_name test2.org;
       listen      8080;
       root        /var/www/photos;
}